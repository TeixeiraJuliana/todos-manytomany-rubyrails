<%= form_with(model: theme) do |form| %>
  <% if theme.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(theme.errors.count, "error") %> prohibited this theme from being saved:</h2>

      <ul>
        <% theme.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title_list %>
    <%= form.text_field :title_list %>
  </div>

  <div class="field">
    <%= form.label :description_list %>
    <%= form.text_field :description_list %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
